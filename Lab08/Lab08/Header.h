#pragma once
#include <iostream>
#include <vector>
#include <stdexcept>
using namespace std;

vector<double> sorting(vector<double> inputVec);		//function declarations

double total(vector<double> inputVec);

double mean(vector<double> inputVec);

double median(vector<double> inputVec);